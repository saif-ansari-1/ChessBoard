## Installations
* Java 11
* Scala 2.13 / SBT 1.3

## Setup

* Run `sbt compile` in the project directory

## Test coverage

* Run `sbt test` in the project directory

## Run App

* Run `sbt "run <input>"` in the project directory. eg `sbt "run King D5"`